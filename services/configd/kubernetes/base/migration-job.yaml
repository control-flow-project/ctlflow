apiVersion: batch/v1
kind: Job
metadata:
  name: configd-migrate
spec:
  backoffLimit: 3
  template:
    spec:
      automountServiceAccountToken: false
      restartPolicy: OnFailure
      securityContext:
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        runAsNonRoot: true
      containers:
        - name: migrate
          image: configd-migrations
          imagePullPolicy: IfNotPresent
          env:
            - name: CTLFLOW_DATABASE_PATH
              value: /var/lib/ctlflow/configd.sqlite
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
            runAsGroup: 65532
            runAsUser: 65532
          volumeMounts:
            - name: data
              mountPath: /var/lib/ctlflow
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: configd-data
        - name: tmp
          emptyDir: {}
