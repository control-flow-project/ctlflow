# Workspace domain placeholder

This directory will own Workspace entities, validated values, lifecycle rules, and
purpose-named results. Add source only with the matching specification, shared
protobuf contract, Knex migration, and canonical integration evidence.

Expected operation files begin with `ResolveWorkspaceResult.cs`; do not add an
unimplemented operation or compatibility type.
