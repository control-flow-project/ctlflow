# Lifecycle gRPC placeholder

This directory will own the direct `GetLifecycle` operation after its exact
request, response, authorization, visibility, and error contract is present in
the specification and shared proto.

Lifecycle administration remains in the aggregated API rather than this direct
operation.
