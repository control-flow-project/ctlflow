# Child-state gRPC placeholder

This directory will own `AcknowledgeChildState` wire translation after the
shared contract defines its typed owner, generation, step, idempotency, and
outcome fields.

The RPC is not added until its real persistence path and canonical test file
exist in the same change.
